export interface FixedPackagesInfo {
  size: string;
  id: string;
  imgWidth: number;
  imgHeight: number;
  altImgWidth: number;
}

export interface SizePerPageTypes {
  name: string;
  value: number;
}